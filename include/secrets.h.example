// Vorlage fuer WLAN-Zugangsdaten.
// Kopieren nach "secrets.h" (im selben Ordner) und echte Werte eintragen.
// secrets.h ist in .gitignore eingetragen und wird NICHT versioniert.
#pragma once

#define WIFI_SSID "dein-wlan-name"
#define WIFI_PASSWORD "dein-wlan-passwort"
